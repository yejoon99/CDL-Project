apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fastapi
  template:
    metadata:
      labels:
        app: fastapi
    spec:
      nodeName: cdl-k8s-control
      containers:
      - name: fastapi
        image: kyee33/cdl_kubeedge_wildfire:fastapi-app
        ports:
        - containerPort: 8001
